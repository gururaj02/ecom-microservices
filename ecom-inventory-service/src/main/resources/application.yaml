spring:
  application:
    name: ecom-inventory-service

  datasource:
    url: jdbc:h2:mem:inventorydb
    username: admin
    password: password
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081